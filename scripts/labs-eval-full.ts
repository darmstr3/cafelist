// Retired 2026-05-14 — replaced by scripts/eval.ts, which runs the
// same in-process pipeline but persists every result to Supabase so
// /labs/eval can plot quality / cost / latency over time and diff
// traces between runs.
//
// Use:  npm run eval

throw new Error(
  'labs-eval-full.ts is retired. Run `npm run eval` instead — see scripts/eval.ts.'
)
