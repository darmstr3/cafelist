# Coverage Gap Report — 2026-06-08

Generated by `scripts/coverage-gap.ts` pipeline (replicated via direct
Supabase SQL — sandbox cannot execute `npm run coverage-gap` due to
filesystem restrictions in the scheduled-task environment).

> Run note: same aggregation logic and `priority_score` formula as the
> TypeScript script; no changes to `scout_priority` this run (see below).

## Window
- **Since:** 2026-06-01T00:00:00Z
- **Through:** 2026-06-08T00:00:00Z
- **Lookback:** 7 days
- **Total logged runs:** 0
- **Distinct (city, neighborhood) groups:** 0

## Method
For each (city, neighborhood) extracted from parsed intent we compute:
- `query_count` — demand signal.
- `avg_quality_score` — evaluator output, 0–10.
- `thin_result_rate` — fraction of runs returning fewer than 3 picks.
- `dominant_failure_mode` — most common non-`ok` failure mode.

```
priority_score = log10(1 + queryCount)
               * max(0, 10 - avgQuality)
               * (1 + thinResultRate)
               * 100    // weight vs hardcoded seeds (seeds cluster 80–100)
```

## Top 0 acquisition priorities

| # | Location | Queries | Avg quality | Thin-result rate | Dominant failure | Priority |
|---|----------|---------|-------------|------------------|------------------|----------|
| — | _(no groups in window)_ | — | — | — | — | — |

No rows upserted into `scout_priority` this run.

## Failure-mode breakdown (all logged runs)

_(none — zero rows in window)_

## Sample queries (top 5 priorities)

_(none)_

## Anomalies

- **Zero logs in the 7-day window.** `agent_query_logs` contains 4 rows
  total, all from 2026-05-20 to 2026-05-25 — nothing logged in the past
  14 days. This is a continuation of the collapse flagged in the
  2026-05-25 report (which had only 1 row vs 11 in the first run). The
  `/labs` query logging path into `agent_query_logs` appears to be broken
  or the route is seeing no traffic. **Recommend:** spot-check the
  `/api/labs` write path and confirm `agent_query_logs` inserts are
  firing for real queries before relying on this pipeline for Scout
  prioritisation.

- **No `coverage_gap` rows in `scout_priority`.** The table has no
  `source='coverage_gap'` entries (the May 25 upsert failed due to the
  known `idx_scout_priority_city_hood` constraint conflict documented in
  cafelist_coverage_gap_upsert_bug.md). Scout is currently running on
  hardcoded seeds only.
