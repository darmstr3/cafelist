# Coverage Gap Report — 2026-06-22

Generated by replicating the `scripts/coverage-gap.ts` pipeline via direct
Supabase SQL — the sandbox could not execute `npm run coverage-gap` this run
(every read of the `tsx` binary and the TypeScript sources off the mounted
project tree failed with `errno -35 / "Resource deadlock avoided"`, a
filesystem-mount instability in the scheduled-task environment, not a script
bug). Same aggregation logic and `priority_score` formula as the TypeScript
script; no changes to `scout_priority` this run (see below).

## Window
- **Since:** 2026-06-15T00:00:00Z
- **Through:** 2026-06-22T00:00:00Z
- **Lookback:** 7 days
- **Total logged runs:** 0
- **Distinct (city, neighborhood) groups:** 0

## Method
For each (city, neighborhood) extracted from parsed intent we compute:
- `query_count` — demand signal.
- `avg_quality_score` — evaluator output, 0–10.
- `thin_result_rate` — fraction of runs returning fewer than 3 picks.
- `dominant_failure_mode` — most common non-`ok` failure mode.

```
priority_score = log10(1 + queryCount)
               * max(0, 10 - avgQuality)
               * (1 + thinResultRate)
               * 100    // weight vs hardcoded seeds (seeds cluster 80–100)
```

## Top 0 acquisition priorities

| # | Location | Queries | Avg quality | Thin-result rate | Dominant failure | Priority |
|---|----------|---------|-------------|------------------|------------------|----------|
| — | _(no groups in window)_ | — | — | — | — | — |

No rows upserted into `scout_priority` this run.

## Failure-mode breakdown (all logged runs in window)

| Failure mode | Count |
|--------------|-------|
| _(no runs in window)_ | 0 |

## Notes / anomalies
- **No new query logs.** `agent_query_logs` holds only 4 rows all-time; the
  most recent is **2026-05-25**. Nothing has been logged in ~28 days, so the
  7-day window is empty. This matches the prior 2026-06-08 run (also 0).
  Consistent with the site being fully anonymous / pre-friend-testing — there
  is essentially no `/labs` traffic generating logs yet.
- **`scout_priority` unchanged:** 20 active rows, all `source='hardcoded'`
  (max priority_score 100.0). No active `coverage_gap` rows. The
  `cafelist-coverage-gap` artifact will render the 20 hardcoded seeds.
- **Execution environment:** could not run the script directly (mount read
  errno -35). Did not retry blindly; replicated the read-only aggregation via
  Supabase SQL, which is non-mutating and safe.
