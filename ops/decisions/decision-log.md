# Operations Decisions (Symlink)

See `docs/DECISION_LOG.md` for the full operations decision log.

This file exists to make the `ops/decisions/` directory discoverable. The canonical log lives at `docs/DECISION_LOG.md`.

Architecture decisions (ADRs) live at the repo root in `DECISION_LOG.md`.
